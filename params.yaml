# Sieu tham so tot nhat sau Buoc 1 (accuracy cao nhat tren tap 2998 mau)
# Voi 5996 mau (Buoc 3), cung bo params dat ~0.754 accuracy
# GitHub secrets configured - retrigger pipeline
n_estimators: 300
max_depth: null
min_samples_split: 2
