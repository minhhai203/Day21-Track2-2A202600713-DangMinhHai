# Bonus 2: chon thuat toan — random_forest | gradient_boosting | logistic_regression
model_type: random_forest
n_estimators: 300
max_depth: null
min_samples_split: 2
max_iter: 500
# retrigger pipeline sau fix rollback float compare
